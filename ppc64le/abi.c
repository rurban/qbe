#include "all.h"

bits
ppc64le_retregs(Ref r, int p[2])
{
	(void)r;
	(void)p;
	assert(0);
}

bits
ppc64le_argregs(Ref r, int p[2])
{
	(void)r;
	(void)p;
	assert(0);
}

void
ppc64le_abi(Fn *fn)
{
	(void)fn;
	assert(0);
}
